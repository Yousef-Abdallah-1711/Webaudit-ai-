/**
 * T241 — the customer app shell, ported from design-system/ui_kits/app/Sidebar.jsx.
 *
 * Import from here, not from a component's own file directly — same
 * discipline as apps/web/components/ui/index.ts, components/report/index.ts,
 * and components/public/index.ts.
 */
export {
  AppShell,
  type AppShellProps,
  Sidebar,
  type SidebarProps,
  PageHead,
  type PageHeadProps,
} from './Sidebar';
