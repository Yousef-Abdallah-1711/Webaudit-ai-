/**
 * T155/T156 — the fixes-board components, ported from `FixesScreen` in
 * `design-system/ui_kits/app/Screens.jsx`.
 *
 * Import from here, not from a component's own file directly — the same
 * barrel discipline as `components/ui`, `components/report`, and
 * `components/dashboard`.
 */
export { FixesBoard, type FixesBoardProps } from './FixesBoard';
export { IssueRow, type IssueRowProps } from './IssueRow';
