Use `Card` for any bounded panel — report areas, pricing tiers, settings blocks.

```jsx
<Card eyebrow="Security" title="7 issues found">…</Card>
```

Leave `elevated` off by default: depth in this system comes from stepping the section background (#ffffff → #fafafa → #f9fafb), not from shadow.
