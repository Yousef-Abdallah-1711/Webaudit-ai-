import * as React from 'react';
/** Uppercase label with the system's signature +1.52px tracking. */
export interface EyebrowProps { tone?: 'muted' | 'accent'; children?: React.ReactNode }
export function Eyebrow(props: EyebrowProps): JSX.Element;
