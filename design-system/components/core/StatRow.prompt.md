Use `StatRow` for the persistent counter above the fixes board and for social proof under the hero CTA.

```jsx
<StatRow items={[{value:3,label:'critical'},{value:5,label:'high'},{value:2,label:'resolved'}]} />
```
