Use `TwoToneHeading` for the hero H1 and for section heads that carry the brand.

```jsx
<TwoToneHeading lead="Think your site is ready?" accent="Prove it, then ship it." />
```

Reproduce the mechanism, not the reference's sentence. One per screen.
