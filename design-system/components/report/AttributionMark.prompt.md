Use `AttributionMark` on every issue, without exception. `<AttributionMark kind="measured" />`.

It is set in mono because mono means machine truth. Never hide it behind a hover or a tooltip-only affordance — 100% of delivered issues must carry visible attribution.
