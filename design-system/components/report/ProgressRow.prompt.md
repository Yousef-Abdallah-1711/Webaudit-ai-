Use `ProgressRow` above the module list on the live scan view. `<ProgressRow phase="Running security checks" elapsed="1:42" done={2} total={5} />`.

Never render indeterminate motion when nothing is happening, and never omit elapsed time.
