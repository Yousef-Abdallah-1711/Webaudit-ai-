Use `ScoreArc` once per report, at the top. `<ScoreArc score={84} delta={23} />`.

It animates exactly once and respects `prefers-reduced-motion` by rendering the final value immediately. Band colour comes from the severity scale, never from the accent.
