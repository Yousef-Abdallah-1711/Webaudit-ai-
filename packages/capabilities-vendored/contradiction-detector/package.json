{
  "name": "@webaudit/capability-contradiction-detector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "T142 — TESTING, CODE layer. Cross-module self-consistency check over priorModuleResults: flags a completed audit's own results not adding up (e.g. a severe finding under a near-perfect score).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@webaudit/capability-sdk": "workspace:*",
    "@webaudit/types": "workspace:*"
  }
}
