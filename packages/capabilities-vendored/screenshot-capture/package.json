{
  "name": "@webaudit/capability-screenshot-capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "T139 — UI, CODE layer. Broken-image detection via fetch, plus best-effort rendered-layout checks (horizontal overflow, tiny tap targets, blank render) when a browser page is available.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@webaudit/capability-sdk": "workspace:*",
    "@webaudit/types": "workspace:*"
  }
}
