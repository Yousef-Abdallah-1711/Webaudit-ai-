{
  "name": "@webaudit/safe-archive",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "R7 — the only way an uploaded archive is inspected or extracted. Every limit enforced before bytes land.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./tests/helpers/build-zip.ts"
  },
  "notes": {
    "./testing": "The hostile-archive builder, for suites in other packages that need to feed one through a route. A named subpath rather than a relative import across a package boundary, and deliberately absent from '.', so nothing in production can construct an archive by accident."
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@webaudit/config": "workspace:*"
  }
}
